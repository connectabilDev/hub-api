# Docker Environment Configuration

# Node Environment
NODE_ENV=development
PORT=3000
DEBUG_PORT=9229

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=connectabil
DB_PASSWORD=connectabil123
DB_NAME=connectabil_db
DATABASE_URL=postgresql://connectabil:connectabil123@postgres:5432/connectabil_db

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRATION=7d

# PgAdmin Configuration (optional - tools profile)
PGADMIN_EMAIL=admin@connectabil.com
PGADMIN_PASSWORD=admin123
PGADMIN_PORT=5050

# Redis Commander Configuration (optional - tools profile)
REDIS_COMMANDER_USER=admin
REDIS_COMMANDER_PASSWORD=admin123
REDIS_COMMANDER_PORT=8081

# MailHog Configuration (optional - tools profile)
MAILHOG_SMTP_PORT=1025
MAILHOG_WEB_PORT=8025

# Nginx Configuration (optional - production profile)
NGINX_PORT=80
NGINX_SSL_PORT=443

# API Configuration
API_PREFIX=api
API_VERSION=v1
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=debug
LOG_FORMAT=json

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DESTINATION=./uploads